public class MissingOperand extends Exception
{
	//3.Missing Operand: If the last term in the input sequence is an operator other than the right parenthesis, it must be the case that the last operation has no second operand. 
	//In this case, print Missing operand and exit the program.
	//if(an operator is followed by another operator or paren. and no operand)
	  // System.out.println("Missing operator");
	public MissingOperand()
	{
	}
}
